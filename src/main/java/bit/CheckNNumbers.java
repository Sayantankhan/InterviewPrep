package bit;

import java.util.*;

public class CheckNNumbers {
    public static int getNewNum(ArrayList<Integer> arr, int n) {
        Collections.sort(arr);

        // 2^0, 2^1, 2^2, 2^3, 2^4
        // 1    2    4     8    16
        return 0;
    }
}
